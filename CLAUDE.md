# Gratry Snow - 開發者指令指南

@AGENTS.md

## 常用指令 (Commands)
- **開發環境:** `npm run dev`
- **正式編譯:** `npm run build`
- **語法檢查:** `npm run lint`
- **安裝依賴:** `npm install`

## 程式碼規範 (Coding Style)
- 遵循 `AGENTS.md` 中的 i18n 規範，絕不硬編碼字串。
- 使用 `shadcn/ui` 作為基礎元件，修改樣式優先使用 Tailwind CSS。